apiVersion: v1
kind: Service
metadata:
  name: terrafusion-api
  namespace: terrafusion-production
  labels:
    app: terrafusion-api
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: terrafusion-api